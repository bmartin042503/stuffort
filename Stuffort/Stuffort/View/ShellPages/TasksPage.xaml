<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:resx="clr-namespace:Stuffort.Resources"
             x:Class="Stuffort.View.ShellPages.TasksPage"
             Shell.TabBarIsVisible="False"
             Shell.BackgroundColor="{StaticResource stuffortBlue}"
             xmlns:converters="clr-namespace:Stuffort.ViewModel.Converters"
             xmlns:fitlabel="clr-namespace:Stuffort.ViewModel.Renderers"
             x:Name="tasksPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="ic_action_add.png"
                     Command="{Binding TaskCommand}"
                     CommandParameter="{Binding SubjectListCount}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:DateTimeToStringConverter x:Key="dtConverter"/>
            <converters:DeadlineConverter x:Key="dlConverter"/>
            <converters:IsDoneConverter x:Key="idConverter"/>
            <converters:IsDoneToIconConverter x:Key="idicConverter"/>
            <converters:LongNameConverter x:Key="lnConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Margin="15, 17.5, 15, 0">
        <ListView x:Name="taskListView" ItemsSource="{Binding TaskList}" IsPullToRefreshEnabled="True"
                  HasUnevenRows="True"
                  RefreshCommand="{Binding TaskRefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Padding="15" HasShadow="True" Margin="0,0,0,15" 
                               CornerRadius="15" BackgroundColor="{StaticResource stuffortBlue}">
                            <StackLayout Orientation="Horizontal">
                                <Grid Margin="-10,0,0,0" Padding="-5" Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="1.3*"/>
                                        <RowDefinition Height="1*"/>
                                        <RowDefinition Height="1*"/>
                                        <RowDefinition Height="0.75*"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.75*"/>
                                        <ColumnDefinition Width="5.5*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button TextColor="{StaticResource stuffortWhite}" 
                                        Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" 
                                        Text="{Binding Path=Item2, Converter={StaticResource idicConverter}}"
                                        Style="{StaticResource fontButton}" Margin="-5,0,-7,0" FontSize="52.75"
                                        Command="{Binding Path=BindingContext.TaskDoneCommand, Source={x:Reference tasksPage}}" 
                                        CommandParameter="{Binding Item1}"/>
                                    <Label Margin="-3,0,0,0" Grid.Row="0" Grid.Column="1" FontSize="20" TextColor="White" Text="{Binding Item1.Name, Converter={StaticResource lnConverter}}">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Path=BindingContext.TapCommand, Source={x:Reference tasksPage}}" CommandParameter="{Binding Item1.Name}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Margin="-3,-7.5,0,0" Grid.Row="1" Grid.Column="1" FontSize="15" TextColor="White" Text="{Binding Item1.SubjectName}"/>
                                    <Label Margin="-3,-10,0,0" Grid.Row="2" Grid.Column="1" TextColor="White" Text="{Binding Item1.DeadLine, Converter={StaticResource dlConverter}}"/>
                                    <Label Margin="-3,-2.5,0,0" Grid.Row="3" Grid.Column="1" TextColor="LightGray" FontSize="12" Text="{Binding Item1.AddedTime, Converter={StaticResource dtConverter}}"/>
                                </Grid>
                                <FlexLayout JustifyContent="SpaceBetween" Margin="-60,0,-35,0" Direction="Column">
                                    <Button Margin="0,-5,0,0" Style="{StaticResource fontButton}" TextColor="White" Text="&#xec55;"
                                        Grid.Column="2" Grid.Row="0" Grid.RowSpan="4" FontSize="20"/>
                                    <Button Margin="0,0,0,-5" Style="{StaticResource fontButton}" TextColor="White" Text="&#xec53;"
                                            Grid.Column="2" Grid.Row="0" Grid.RowSpan="4" FontSize="20" Command="{Binding Path=BindingContext.TaskRemoveCommand, Source={x:Reference tasksPage}}"
                                            CommandParameter="{Binding Item1}"/>
                                </FlexLayout>
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>